# Item Images

This directory contains downloaded item images (weapons, armor, accessories).

Images are downloaded when running the scraper with:
```bash
npm run scrape:items
```

Image naming convention: `{item_id}_{item_name}.{extension}`

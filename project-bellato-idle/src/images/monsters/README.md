# Monster Images

This directory contains downloaded monster images.

Images are downloaded when running the scraper with:
```bash
npm run scrape:monsters
```

Image naming convention: `{monster_id}_{monster_name}.{extension}`
